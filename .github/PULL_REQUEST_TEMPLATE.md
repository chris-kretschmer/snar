## What does this change?

<!-- Short description of what and why. -->

## Checklist

- [ ] `node --check src/*.js` passes
- [ ] Tested manually (clicked through the affected UI, or a `curl` round-trip for server-only changes)
- [ ] Comments explain the *why*, not the *what* (only added where non-obvious)
- [ ] No new dependency/framework/build step introduced without prior discussion in an issue
